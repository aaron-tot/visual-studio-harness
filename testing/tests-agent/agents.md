FYI - Agent tests never seem to be relieable without careful direction from user durion creation on tests.

This folder contains either free form tests created by agents or cloned tests modified but not 100% varified by the user. "cloned_"

Agents, anything with cloned_ should not be changed without explicit permission from the user.


any depndancies needed should be cloned into this folder if you need to edit them. if you dont need to edit them, then you can use the orignal user deps. anytime you import a dep, you should have a comment above it explainnig that it is the org and cant be edited and if edits are needed to clone it in the agents test folder