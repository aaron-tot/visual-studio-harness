{
  "name": "_backend",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nodemon --watch src --watch ../_shared -e ts --signal SIGTERM --exec 'MODE=dev bun run src/index.ts'",
    "build": "bun run ../scripts/build-prod.ts",
    "start": "bun run ../scripts/run-prod.ts",
    "test": "bun test src"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.7",
    "@fastify/cors": "^10.0.0",
    "@fastify/static": "^8.0.0",
    "@fastify/websocket": "^11.0.0",
    "@parcel/watcher": "^2.6.0",
    "ai": "^7.0.22",
    "fastify": "^5.0.0",
    "gpt-tokenizer": "^4.0.0",
    "jsonrepair": "^3.15.0",
    "sqlite-vec": "^0.1.9",
    "ws": "^8.21.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/ws": "^8.18.0",
    "nodemon": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
