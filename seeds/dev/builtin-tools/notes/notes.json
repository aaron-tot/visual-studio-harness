{
  "name": "notes",
  "description": "Create, read, update, and archive user notes. Set 'action' to pick the operation.",
  "entry": "index.ts",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "read",
          "create",
          "update",
          "archive",
          "move"
        ],
        "description": "Operation: read, create, update, archive, move"
      },
      "name": {
        "type": "string",
        "description": "Note name"
      },
      "title": {
        "type": "string",
        "description": "Title"
      },
      "body": {
        "type": "string",
        "description": "Body"
      },
      "scope": {
        "type": "string",
        "enum": [
          "global",
          "project",
          "session"
        ],
        "description": "Scope"
      },
      "fromScope": {
        "type": "string",
        "enum": [
          "global",
          "project",
          "session"
        ],
        "description": "Source scope (move; omit to resolve existing)"
      },
      "toScope": {
        "type": "string",
        "enum": [
          "global",
          "project",
          "session"
        ],
        "description": "Target scope (move)"
      }
    },
    "required": [
      "action"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  },
  "enabled": true,
  "permissionDefault": "allow"
}
